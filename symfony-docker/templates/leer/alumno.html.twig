{% extends 'base.html.twig' %}

{% block title %}Seleccionar alumno
{% endblock %}

{% block body %}
	{{ parent() }}
	<!DOCTYPE html>
	<div class="container">
		<div class="row">
			<div class="col-md-6">
				<h1 class="font-weight-bold">Introduce el dni: </h1>
				<form action="{{ path(controlador) }}" method="POST">
                    <input type="text" class="form-control dniAlum" id="dniAlum" name="dniAlum" placeholder="Ejemplo: XXXXXXXXXG">
					<button type="submit" class="btn btn-primary">{{accion}}</button>
				</form>
			</div>
		</div>
	</div>

	<footer class="footer mt-auto py-3">
        <div class="container">
            <div class="text-left">
                <a href="{{ path('app_menu_alumno') }}" class="btn btn-primary">Volver al menú alumno</a>
            </div>
        </div>
    </footer>
{% endblock %}
